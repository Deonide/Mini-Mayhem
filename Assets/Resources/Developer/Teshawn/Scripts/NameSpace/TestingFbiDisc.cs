using UnityEngine;

public class TestingFbiDisc : MonoBehaviour
{
    //[SerializeField]
    //GameObject obj;

    //float distline;

    //Vector3 offset =  new Vector3(0,0,-3.1f);

    //int amount = 7;

    void Start()
    {

        
    }

    //private void Circle(float rad)
    //{
    //    Vector3 targetpos = offset;

    //    for (int i = 0; i < amount; i++)
    //    {
    //        GameObject instance = Instantiate(obj);

    //        float angle = i * (2 * Mathf.PI / amount);

    //        float x = Mathf.Cos(angle) * rad;
    //        float z = Mathf.Sin(angle) * rad;

    //        targetpos = new Vector3(targetpos.x + x, 0, targetpos.z + z);

    //        instance.transform.position = targetpos;
    //    }
    //}


}
